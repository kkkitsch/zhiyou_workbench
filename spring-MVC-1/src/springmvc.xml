<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">


	<!-- 扫描包 -->
	<context:component-scan base-package="com.springmvc.test"></context:component-scan>
	<context:component-scan base-package="com.springmvc.handler"></context:component-scan>
	<context:component-scan base-package="com.springmvc.view"></context:component-scan>

	<!-- 默认视图解析器:逻辑视图，物理视图 -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 逻辑视图：hello 解析为物理视图：/hello.jsp -->
		<property name="prefix" value="/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>

	<!-- 配置自定义视图解析器 :BeanNameViewResolver -->
	<bean class="org.springframework.web.servlet.view.BeanNameViewResolver">

		<!-- 配置解析器的优先顺序 ：value值越小，优先级越高 -->
		<property name="order" value="101"></property>
	</bean>


	<!-- 
	如果要跳转的视图当中包含JSTL的标签，则要使用InternalResourceViewResolver的 
	子类ResourceBundleMessageSource来实现视图解析 
	-->
	<!-- 必须指明id，而且id=messageSource -->
	<bean id="messageSource"
		class="org.springframework.context.support.ResourceBundleMessageSource">

		<!-- 国际化配置文件资源的前缀 -->
		<property name="basename" value="jstl"></property>
	</bean>


	<!-- 给某个个页面配置了映射信息 -->
	<!-- 前提：导入mvc命名空间 -->
	<!-- 
	如果给某个个页面配置了映射信息，那么其他的控制器方法映射信息全部无效，
	解决方法：加上 <mvc:annotation-driven></mvc:annotation-driven> 
	-->
	<!-- path:路径，view-name ：逻辑视图名称 -->
	<mvc:view-controller path="/abcdefg" view-name="jstl" />
	<mvc:annotation-driven></mvc:annotation-driven>
	<!-- 给自定义视图配置映射信息 -->
	<mvc:view-controller path="/mmmyview" view-name="myView" />

</beans>
